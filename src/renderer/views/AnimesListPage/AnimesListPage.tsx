import { NavLink } from 'react-router-dom';

const AnimesListPage = () => {
  return (
    <div>
      <h1>Tous les Animes</h1>
      <NavLink to="/">Accueil</NavLink>
    </div>
  );
};

export default AnimesListPage;
